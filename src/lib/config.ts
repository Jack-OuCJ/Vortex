/**
 * AI 额度配置
 *
 * 修改此处会影响前端展示逻辑。
 * 注意：数据库迁移文件 supabase/migrations/20260402_add_ai_balance.sql
 * 中的 DEFAULT 值需要与 DEFAULT_AI_BALANCE 保持一致。
 */
export const DEFAULT_AI_BALANCE = 500;
export const MAX_AI_BALANCE = 500;
